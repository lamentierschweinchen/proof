# Hugo build output
site/public/
site/resources/
site/.hugo_build.lock

# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~

# Node (not used but just in case)
node_modules/

# Design source files (not part of the publication)
Design/
Description/
Plan/
Protagonists/
.vercel

# Operational layer — private by design
# (identities/ is published; the working folders and logs are not.
#  archive.json is the canonical public record and stays tracked.)
poet/
rowan/
ossian/
editor/
editor_log/
/editions/
current_edition.json
human_copy*.md
proof_complete_setup.md

# Local tooling
.claude/
